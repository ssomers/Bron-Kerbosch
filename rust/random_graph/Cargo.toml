[package]
name = "random_graph"
version = "0.1.0"
authors = ["Stein Somers <git@steinsomers.be>"]

[dependencies]
bron_kerbosch = { path = "../bron_kerbosch" }
rand = "0.6.0"

[dev-dependencies]
rand_chacha = "0.1.0"
